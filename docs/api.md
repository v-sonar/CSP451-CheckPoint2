# API Endpoints

## Description
- GET /api/users → list all users
- GET /api/users/:username → get single user
- POST /api/users → create new user
- All routes use database helper functions

## Usage
- Use a tool like Postman or curl to test endpoints
- Ensure database has users before GET requests
