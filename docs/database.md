# Database Connection

## Description
- Uses SQLite to store users
- Provides helper functions: addUser, getUser, listUsers
- Optional test route: `/db-test` to list all users

## Usage
- Call helper functions in API or routes
- Test route GET /db-test returns list of all users
